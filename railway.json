{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "cd backend && .venv/bin/uvicorn server:app --host 0.0.0.0 --port $PORT",
    "restartPolicyMaxRetries": 5
  }
}
